# 踩过的坑

这个项目大部分时间不是在写功能，是在发现**我以为在工作、其实没在工作**的东西。下面这些跟本项目关系不大，但形状是通用的。

---

## 一、闸测的是产品，还是产品的副本？

**同一个错犯了三次。**

1. `--selftest-record` 自己复制了一份录音逻辑（两处独立读 `recorder.currentTime`）。于是生产那处的 bug 重新犯一次，这一项照旧绿——**而它正是专为那个 bug 加的**。
2. 流式播放的自检拿**自己造的立体声 buffer** 测解交错器，绿得好好的。而那条音频流其实是单声道，放出来全是噪音。闸抄了实现的假设，就失去了独立性。
3. 收尾闸编译到 `build/dev/`，却去拷问三小时前的 `tingzhe.app`。**编译 A、拷问 B。**

> 判据：这一项**驱动的是生产代码路径**，还是一条为它准备的平行路径？
> 如果断言用的常量和实现用的常量是同一个来源，那它证明不了任何事。

## 二、没有东西依赖的警告，等于不存在

- 文档写着「🟡 不算全绿」，而 `🟡` 的退出码是 `0`，`pre-push` 只看退出码。**那句话没有任何东西依赖它。**
- 「裸键会误触发」写成了警告 → 照样装上 → 打字时乱触发。
- 「只在代码定稿时才正式构建」这条铁律**只写在注释里**，写下它的当天就被违反了 8 次。

> 判据：如果这句话是假的，**会有什么东西变红吗**？没有的话它就是装饰。

## 三、一个不改变任何行为的控件，比没有控件更糟

界面上三档「收音灵敏度」，实际计算是 `门槛 = max(绝对下限 0.02, 环境底噪 × 倍数)`。
而真实环境底噪只有 0.0007–0.0060 ⇒ `底噪 × 倍数` **永远小于** 0.02 ⇒ **门槛恒等于出厂值**。

三个按钮从灵敏拧到迟钝，门槛一动没动。用的人不会怀疑控件，只会得出结论「调到最钝了还是不行」。

> 加控件时问一句：**在真实参数范围里，这几档给出的值真的不同吗？** 这条可以写成断言。

## 四、`pipefail` + `| grep -q`（本仓第 4、5 次现身）

```bash
if launchctl list | grep -q foo; then ...   # ⛔
```

`grep -q` 命中即关管道 → 上游吃 `SIGPIPE` 返回非零 → 开了 `set -o pipefail` 就**整条判假** → 分支永远跳过。

后果：脚本一直声称「重启常驻以载入新二进制」，实际那段**从写下起一次都没执行过**。

> 铁律：管道结果**先落变量再判**，别在 `if` 里直接接 `| grep -q`。

## 五、`$VAR` 后面紧跟中文，bash 会把中文吃进变量名

```bash
echo "已换新进程（PID $OLDPID → $NEWPID）"     # ⛔ 它找的是变量 NEWPID）
```

配上 `set -u` 就是 `unbound variable`，脚本当场死。写 `${NEWPID}` 即可。

它活了一整天，因为**那一行只在"重启常驻"分支上**，而那条分支只有正式构建才走。
> **只在少见分支上的语法坑，跑不到就等于没写过。** 拿静态扫描兜住它。

## 六、macOS TCC：授权到底绑在什么上

- `codesign --sign -` 是 **ad-hoc**，它的 designated requirement **就是 CDHash 本身** → 改一个字节，辅助功能授权就失效。
- 用 **本机自签身份**（`openssl` 造证书 + `security import`，不要 Apple ID、不花钱、不联网）签名，designated 变成
  `identifier "…" and certificate leaf = H"…"` —— **认 app + 证书，与二进制内容无关**，授权跨构建存活。

我曾在文档里写下「本机 0 valid identities，**现在没有这条路**」，让人重授了 8 次。
那句话是我的结论，不是事实——**我从没试过自己造一张证书**。

> 铁律：任何「X 做不到／不存在」的断言，必须附一句**「我试过什么」**，
> 否则它只是印象，而它会变成后续所有推理的地基。

## 七、同一份真相存两处，你只会改一处

「构建会打掉授权」这句话在脚本里有两份：构建前的确认框、构建后的提示框。
自签名上线后我改对了第一份，**漏了第二份**——于是每次构建仍然弹出一个大框，催人去做一件完全不必要的事。

> 治法：让闸去查「第二份还在不在」。真相的**份数**本身就是可以断言的。

## 八、假警报比没有警报贵

闸判红说「自检碰了生产状态位」，实际是**用的人在那 40 秒里自己按了一下开关**。
这类断言分不清「程序动的」和「人动的」。

> 下一次真红，人会先怀疑闸。**让闸只对它真能分辨的事情说话。**

## 九、静默退化

- 麦克风的噪声底估计**无条件吸收所有帧**，于是一段话被切断后，你继续说的声音被当成"环境噪音"抬高了门槛 → 你更进不来 → 吸收得更多。**正反馈，越说越聋。**
  修法：升降不对称——比当前底噪低的帧快速跟下去，只有判定为"不是人声"的帧才允许缓慢抬高，**人声一律不吸收**，再加一个绝对上限（涨到听不见人之后，就再也没有帧能证明它太高了）。
- 一个 launchd 直接 exec 的裸二进制**拿不到菜单栏**（`System Events` 查得 `menu bars = 1`，正常 app 是 2），而 `statusItem.isVisible` 照样返回 `true`。**我造了一个会说谎的仪器，然后信了它三轮。**

## 十、格式不能从"隔壁那条路"推

同一个 TTS 服务：非流式 `wav` 是 **48kHz 立体声**，流式 `pcm` 是 **48kHz 单声道**。
我拿前者的头去推后者的格式，按立体声解交错单声道数据 → 半速 + 噪音。

判定用了两条互不依赖的实测判据，都不靠耳朵：
- 交错立体声里相邻一对是同一时刻的左右声道，几乎相同：参照 291.6，这条流 1.87
- 基频：参照 162.7 Hz，按 48k 单声道解得 174.5 Hz（同一个嗓子），按 24k 解得 87.3 Hz（低一个八度）

> 配错格式的产物是**噪音**，而噪音**不会让任何断言变红**。
> 所以另外装了一个统计判别器：第一块数据到达时核对声道数，对不上就在日志里明说。

## 十一、别替用的人做决定

我曾拦下三类快捷键：跟别的键撞的、会打出字的裸键、跟按住说话冲突的。
那三类**全都能装上，只是可能烦**。拦掉它们，是我替一半我根本不认识的人做了决定。

现在只剩一条判据：**这把键装不装得上**。其余一律装，代价写在旁边。
