# 复制成 .env.local 再填 —— .env.local 永不入库（.gitignore 守着，check.sh 也查）。
#
# 默认服务商是 MOSS / MOSI：https://platform.mosi.cn
# 换成任何 OpenAI 兼容的服务：这里填那家的 key，再改 config.json 里的 api_base。
# 代码里没有第二处要改的地方 —— 转写走 /v1/audio/transcriptions，朗读走 /v1/audio/speech。

TINGZHE_API_KEY=
